# `typst` dir

This directory is intended to held the resume building recipe (through
[`typst`](https://typst.app/) program) and it's required data (at `yaml`
files).

The resume outputs can be found at [resumes](../resumes/) directory.
